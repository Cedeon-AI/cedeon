"""Cedeon domain layer.

Pure business types and rules. This package MUST NOT import FastAPI, SQLAlchemy,
Alembic, HTTP clients, the job runner, or any other infrastructure. Enforced by an
import-linter contract (see pyproject.toml).
"""
