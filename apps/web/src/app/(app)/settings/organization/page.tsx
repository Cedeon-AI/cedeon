import { SettingsOrganizationView } from "@/components/app/settings-organization-view";

export const metadata = { title: "Organization settings" };

export default function Page() {
  return <SettingsOrganizationView />;
}
